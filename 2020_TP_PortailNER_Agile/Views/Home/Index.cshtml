@{
    ViewBag.Title = "Home Page";
}
<div class="jumbotron">
    <h1>Nouvelles énergies renouvelables</h1>
</div>

<div id="piechart"></div>

<div class="col-md-3">
    <input type="button" class="btn btn-info" value="Detail NER" onclick="location.href='@Url.Action("DetailNER", "Home")'" />
</div>

<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
<script type="text/javascript">
    // Load google charts
    google.charts.load('current', { 'packages': ['corechart'] });
    google.charts.setOnLoadCallback(drawChart);

    // Draw the chart and set the chart values
    function drawChart() {
        var data = google.visualization.arrayToDataTable([
            ['Energies', 'KWh par jour'],
            ['Nouvelles Energies Renouvelables', 2466],
            ["Hydraulique au fil de l'eau", 14733],
            ['Hydraulique à accumulation', 19062]
        ]);

        // Optional; add a title and set the width and height of the chart
        var options = { 'title': '', 'width': 555, 'height': 400 };

        // Display the chart inside the <div> element with id="piechart"
        var chart = new google.visualization.PieChart(document.getElementById('piechart'));
        chart.draw(data, options);
    }
</script>